import type { CollectionConfig } from 'payload'

export const {{pascalCase name}}: CollectionConfig = {
  slug: '{{lowerCase name}}',
  fields: [
    {
      name: 'title',
      type: 'text',
    }
  ]
}
